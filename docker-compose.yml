# docker/docker-compose.yml
version: '3.8'

services:
  client:
    build:
      context: ..
      dockerfile: docker/Dockerfile
    volumes:
      - ../configs:/app/configs
    environment:
      - CONFIG_FILE=/app/configs/config.example.json
    restart: unless-stopped
